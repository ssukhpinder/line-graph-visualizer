import { NodeGraphData, CommonDataTypes } from "../../CommonDataTypes";
import { CreateGraphEdge } from "./createGraph";
/**
 * Given a labeled list of roots, it creates a graph by following their edges recursively.
 * Tracks cycles.
 */
export declare function createGraphFromPointers<T>(roots: Record<string, T | undefined | null>, infoSelector: (item: T) => {
    id?: string | number;
    edges: CreateGraphEdge<T>[];
} & Omit<NodeGraphData, "id">): CommonDataTypes.Graph;
//# sourceMappingURL=createGraphFromPointers.d.ts.map